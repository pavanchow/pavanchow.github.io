# Sensitive material. GitHub Pages serves this whole tree including dotfiles
# (upload-pages-artifact path: '.'), so anything committed here is public.
.ssh/
id_rsa*
id_ed25519*
*.pem
*.key
.env
.env.*

# Local backups of pre-change states
*.pre-geo-backup
*.pre-reset.bundle
